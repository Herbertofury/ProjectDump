# Project Constellation Operating Contract

Project Constellation is ACTIVE and runs in NORMAL_OPERATION. Recovery/restoration is fallback-only after a verified integrity failure.

## Startup order

1. Read this file and `HANDOFF.md`.
2. Read `ACTIVE-CHECKPOINT.json` and `Project-Constellation-Automation-State.json`.
3. Use the dedicated Google Drive `Project Constellation` folder ID `1mUSeFlumpIOBtVCmlwbhN11kChRsdblV` for the canonical presentation artifacts and 63-project catalog.
4. Compare the Drive artifact hashes against `lastAutomationHash` before editing. If a newer user-edited artifact exists, it is the merge base.
5. Continue normal research rotation and additive product improvement. Do not begin with a recovery sweep merely because sandbox/container files are absent.

## Hard invariants

- Exactly 63 tracked projects unless the user explicitly changes the list.
- The older 25-project `USER-PROJECTS-DATABASE.md` is historical source detail only and cannot replace the active 63-project catalog.
- Preserve user notes, checkpoints, version corrections, next actions, research keep/dismiss/promote decisions, and completed work.
- Missing ephemeral files are not evidence that Project Constellation itself is in recovery.
- The file `Project-Constellation-HISTORICAL-Recovery-Bundle-2026-08-15-SHA256.txt` is historical evidence only.
- After meaningful work, publish same-name canonical artifacts to Drive and verify their bytes before updating the automation state.
