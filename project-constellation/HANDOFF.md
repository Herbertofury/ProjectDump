# Project Constellation Active Handoff

Project Constellation is **ACTIVE** in **NORMAL OPERATION** at **v0.5.0**. Missing ephemeral files are not a restoration condition.

## Startup

Read the root `AGENTS.md`, then this directory's `AGENTS.md`, `PROJECT.json`, `STATUS.json`, `Project-Constellation-Automation-State.json`, `ACTIVE-CHECKPOINT.json`, and the Drive-published `Project-Constellation-Project-Catalog.json`. Locate same-named artifacts in Drive and compare their hashes with `lastAutomationHash`. If a newer user-edited artifact exists, merge from it and preserve the user's state.

## Current product

The v0.5.0 line contains exactly 63 tracked records and keeps It retains quick checkpoints, resume capsules, Ideas & Research decisions, snapshot export/import, and the permanent zero-setup Quick View. This pass adds persistent Continuity Lenses, an explicit Today queue, the Review Queue with reviewed/defer controls, Changed Since Last Visit, legacy v0.4.x local-state migration, research promotion that sets the exact next action, 63 standalone wiki pages, and a self-contained master continuity command center.

The first 25 records preserve the durable 2026-08-09 project database detail. The remaining 38 current continuity records are evidence-backed project tracks assembled from durable artifacts and previously verified Project Constellation continuity because the exact prior v0.4.2 63-entry catalog bytes were not found. Their metadata can be refined by newer project-owned evidence without dropping user state or silently changing the 63-record list.

## Research cursor

This pass refreshed PRJ-014, PRJ-015, PRJ-016, PRJ-019, and PRJ-020, plus the global Continuity Lenses app research. Next run should rotate away from those unless fresh evidence materially changes them.

## Verification

Static validation passed for 63 records, JavaScript syntax, JSON parsing, goal/requirements/history presence, research contract rendering data, local wiki links, hash agreement, and both ZIP integrity checks. All four presentation artifacts were re-downloaded from Drive and SHA-256 matched. Chromium was attempted but timed out with D-Bus runtime errors before producing a DOM, so browser interaction is not claimed.

## Tracked-project wiki maintenance checkpoint

ProjectDump maintains source-controlled Markdown wikis under `wikis/` for projects tracked inside Project Constellation. Project Constellation itself is the control plane and is excluded from the target-project coverage count; its pre-existing PCX-036 continuity page is preserved only as a legacy control-plane reference. The current detailed target-project wiki coverage is **42 tracked projects**.

Latest verified addition: `PRJ-005 - Mascot / Screenmate Platform`. The new umbrella wiki resolves the current browser-side implementation host to `Herbertofury/Gamesync` version `0.6.3`, observed `main` commit `a8e37976eb0b3ee3c4ec5e802b02d3bfa1f41928`, while preserving the unresolved boundary that one standalone repository owning the entire browser + desktop mascot umbrella has not yet been proven. It documents `app/Mascot_Engine.js`, the modular `app/content/mascot/` compatibility layer, the bundled `Shimeji Browser Engine`, `Voicepacks/`, Shimeji/Petz manifest resources, character presets, the unified settings contract, pack lifecycle, state/timeline/memory APIs, engine normalization, browser install/build workflow, extension points, persistence requirements, troubleshooting, and a concrete multi-engine real-browser qualification matrix. It explicitly preserves Shimeji, ACS and Petz engine semantics rather than treating them as interchangeable sprite skins. The remote ProjectDump wiki and updated wiki index were fetched back after publication and verified.

`PRJ-005` is indexed at `wikis/PRJ-005-mascot-screenmate-platform.md`. Target-project coverage is now **42** detailed tracked projects.

Prior verified addition: `PRJ-006 - ACS Agent Parity Runtime`. The new wiki replaces the old unresolved-repository assumption with verified current project-owned evidence from `Herbertofury/Gamesync` version `0.6.3`, observed `main` commit `a8e37976eb0b3ee3c4ec5e802b02d3bfa1f41928`. It documents the shipping ACS binary parser, JavaScript decompressor and optional legacy Wasm acceleration, ACS-to-clippy spritesheet/agent/audio conversion, ACS-to-Shimeji action/behavior mapping, Manifest V3 service-worker integration, `Mascot_Engine.js` engine/message/settings contract, relevant manifest resources, exact build/install commands, parser/converter modification rules, corpus-based import qualification, request-state requirements, `GestureAt()` and `Think()` acceptance, command/recognition boundaries, troubleshooting, and a concrete parity audit plan. It explicitly distinguishes verified parser/converter/integration source from still-unproven complete Microsoft Agent request/Commands/recognition/lip-sync/runtime parity. The remote ProjectDump wiki was fetched back after publication and verified.

`PCX-060 - ACS Voice / Speech Runtime` also received a material freshness correction. Its prior text said a current authoritative ACS voice source was unresolved. Current GameSync `0.6.3` evidence now proves an active host through `Mascot_Engine.js`, the ACS parser/converter pipeline, `Voicepacks/`, manifest resource exposure, and service-worker integration. The refreshed wiki documents that source boundary, current voice-facing settings, embedded ACS audio path, speech/thought/listening qualification requirements, provider-adapter boundaries, build path, runtime matrix, troubleshooting, and relationship to PRJ-006 while still refusing to claim unverified Wait/Interrupt/StopAll, `Think()`, recognition, or exact lip-sync parity. The remote corrected wiki was fetched back and verified.

`PRJ-006` is indexed at `wikis/PRJ-006-acs-agent-parity-runtime.md`. `PCX-060` remains indexed at `wikis/PCX-060-acs-voice-speech-runtime.md`. The target-project wiki index was reconciled to **41** detailed tracked projects at that historical checkpoint.

Prior verified addition: `PCX-056 - GameSync Mod Health`. The new wiki is grounded in current `Herbertofury/Gamesync` source at GameSync `0.6.3` and observed `main` commit `a8e37976eb0b3ee3c4ec5e802b02d3bfa1f41928`, plus the Project Constellation goal to detect meaningful mod-health/version/source changes without destructive automation. It documents the shipping `ModHealthEngine`, the per-game `gsModHealthDb` source-sync database, CSV/text ingestion and severity-preserving merge rules, Nexus Posts/Bugs evidence acquisition, in-memory plus IndexedDB evidence caching, 2-hour evidence TTL, 30-minute in-memory background-job TTL, Quick/Standard/Deep scan profiles, 90-day default window, bounded 1-20 page scan cap, NLP-assisted issue clustering, reporter/frustration/error/expertise confidence boosts, Green/Yellow/Red thresholds, trend persistence and alerts, generated issue/report templates, AutoNotes handoff, overlay architecture, the complete `MODHEALTH_*` message API, build/install workflow, modification map, troubleshooting, preservation rules, and a concrete live-runtime qualification checklist. The shipping `package.json` does not currently declare a dedicated Mod Health test script, and this documentation pass did not run a fresh Opera/Nexus/source-sheet/AutoNotes end-to-end scan, so fresh runtime success is explicitly not claimed.

`PCX-056` is indexed at `wikis/PCX-056-gamesync-mod-health.md`. The target-project wiki index was reconciled to **36** detailed tracked projects at that historical checkpoint.

Prior verified addition: `PCX-055 - GameSync Script Polling Runtime`. The new wiki is grounded in current `Herbertofury/Gamesync` source at GameSync `0.6.3` and observed `main` commit `a8e37976eb0b3ee3c4ec5e802b02d3bfa1f41928`. It documents the real Manifest V3 `chrome.alarms` scheduler, one-pass overlap guard, persisted due-source calculation, hourly/daily/weekly/manual source intervals, per-source failure isolation, durable poll logs, batch catalog processing, IndexedDB Script Tracker stores, status aggregation precedence, Scarlet's Realm and Custom List adapters, the `SCRIPT_*` background API, subscriptions, advisory integration, CSV/JSON import, full export/import, build path, adapter extension workflow, troubleshooting, and a concrete runtime qualification checklist. The Project Constellation requirement for reliable startup catch-up remains explicitly open: source proves that persisted `lastPollAt` timestamps let an overdue source be detected the next time `runScheduledPolls()` runs, but this documentation pass did not prove a browser/service-worker startup path that always invokes that catch-up after a missed window. Fresh live-browser scheduler qualification was not claimed.

`PCX-055` is indexed at `wikis/PCX-055-gamesync-script-polling-runtime.md`.

Prior verified addition: `PCX-052 - GameSync Release Factory`. The new wiki is grounded in the current `Herbertofury/Gamesync` shipping extension at version `0.6.3` and observed `main` commit `a8e37976eb0b3ee3c4ec5e802b02d3bfa1f41928`, plus `Herbertofury/GameSync-Next` at observed `main` commit `9e337c720f0180cffa577f140b181c699f0a1650` with Extension V2 declaring version `0.8.0`. It documents the shipping `app/` -> generated `dist/` Vite build closure, Extension V2 WXT build and ZIP paths, current workspace build graph, parity audit, lint/regression/Playwright lanes, Opera GX verification, same-ID upgrade validation across `chrome.storage.local`, `chrome.storage.sync`, IndexedDB and restart, extension identity expectations, secret scanning, release-ledger fields, packaging boundaries, troubleshooting, and a step-by-step release procedure. The documentation pass inspected repository-declared mechanisms remotely and does not claim that a fresh npm build, Opera run, ZIP package, or product release was executed during this wiki update.

`PCX-052` is indexed at `wikis/PCX-052-gamesync-release-factory.md`, and the published remote file was fetched back after creation to verify the intended documentation is present.

Prior verified addition: `PCX-054 - GameSync Source Finder / Entity Resolver`. The wiki is grounded in the current `Herbertofury/Gamesync` shipping source at GameSync `0.6.3`, not only the reconstructed catalog summary. It documents the current provider-aware source-discovery architecture, more than 25 provider keys, Sims-aware provider filtering, direct/profile/search/tag URL classification, the legacy-compatible discovery pipeline, Source Finder Service, Hybrid Matcher, FlexSearch recall, optional FTS5/SQLite ranking, shared ranking weights, WASM scoring fallback, contradiction blocking, auto-attach/suggested/weak/rejected thresholds, false-positive handling, evidence and score breakdowns, AutoNotes `AUTONOTES_FIND_SOURCES` integration, semantic reranking, library patching, download-candidate scoring, provider-extension workflow, performance constraints, troubleshooting, and the verified `npm ci` / `npm run dev` / `npm run build` development path. The documentation explicitly does not claim fresh live-provider qualification across every supported provider or universal configuration of optional Typesense/Meilisearch backends.

Prior verified addition: `PCX-053 - GameSync Bounty / Rewards Runtime`. The wiki is grounded in the current `Herbertofury/Gamesync` 0.6.3 shipping source at observed commit `a8e37976eb0b3ee3c4ec5e802b02d3bfa1f41928`, plus project-owned Bounty architecture and validation evidence under `Herbertofury/GameSync-Next/Bounty`. It documents the full Bounty source layout, views, IndexedDB stores, authority rules, current adapter registry, authorized-browser-session extraction, Steam ownership reconciliation, Battle.net account-page evidence, Twitch viewer/miner lifecycle, health recovery, stop behavior, calendar/deadline model, artwork provenance, sync pipeline, lossless rendering/performance contract, install/build/test/benchmark workflow, modification map, historical Opera GX validation, regression history, troubleshooting, and open provider/parity gaps. Current shipping source now registers Twitch viewer, Steam library, and Battle.net adapters and therefore supersedes parts of the older July source-coverage matrix; fresh post-change real-Opera provider qualification remains explicitly pending rather than inferred.

The wiki index was also reconciled with files already present in `wikis/`, adding previously unindexed `PCX-031 Auralis`, `PCX-032 Profit Nova`, and `PCX-035 MO2R` entries.

Prior verified addition: `PCX-040 - Pinterest Nocturne`. The wiki is grounded in the exact Drive-published v1.9.0 source archive plus the project-owned UltraDeck recovery repository. The documentation pass downloaded the 400,272-byte source ZIP, recorded SHA-256 `e2fa38831c8969fc4a8b1919f92a69e1e50dec607a5c6425ee2e3b5a6734a8d7`, verified ZIP integrity, passed `npm run build`, `npm run test:static`, and `npm run package`, and documented the manifest, popup/settings, MAIN-world page engine, Lean Browsing, Deep Horizon, media-gated idle runway, Interaction Priority behavior, build/package/test commands, browser installation, modification map, correctness invariants, and troubleshooting. The current-run `test:interaction` lane exceeded the execution window, so fresh runtime interaction success is not claimed.

`PRJ-025 - UltraDeck` also received a material freshness update after the canonical repository began staging a v8.2.0 multi-part source bootstrap. The transfer advanced through commit `da8730240779d1e2508737a0b8c5eee38a0bd11d`, message `Stage UltraDeck v8.2 bootstrap 005/018`, exposing `v82.b64.000` through `v82.b64.004`. v8.2 is therefore recorded as an incomplete in-progress successor and is not promoted over the fully described v8.1.0 line until all 18 parts are reconstructed and verified.

Wiki index: `wikis/README.md`.

## Next pass

Continue normal Project Constellation operation from this checkpoint. Do not begin with a broad restoration sweep.
