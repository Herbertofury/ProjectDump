name: Publish Potatoptimize Forge 1.20.1 Certified Release

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  publication-held-for-final-recertification:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Publication is intentionally held until the final source head completes clean, compatibility-stack, and aggressive-profile certification.'
